
const width = 40;
const height = 40;

var Config = {
    MAZE_WIDTH: width,
    MAZE_HEIGHT: height,
    CANVAS_WIDTH: width * 25,
    CANVAS_HEIGHT: height * 25,
    RERENDER_INTERVAL: 200,
    ACTIONS_INTERVAL: 400,
    PERSON_SIZE: 40,
    BG_SIZE: 35,
}